# Service token is not required to load the skeleton app. Set it in MCP server settings only when you want to embed a dashboard.
# Optional: if your MCP or workflow writes a token here, set it below.
# DATABRAIN_SERVICE_TOKEN=

# Optional: override API base (default https://api.usedatabrain.com)
# DATABRAIN_API_BASE_URL=https://api.usedatabrain.com

# Server
PORT=3002
